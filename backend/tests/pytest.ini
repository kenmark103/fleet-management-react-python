[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Short traceback for CI readability, full for local debugging
addopts = --tb=short -q

markers =
    integration: marks tests as integration tests (require DB)
    e2e: marks tests as end-to-end tests